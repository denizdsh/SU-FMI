installed.packages()
# install.packages(“____”) (“UsingR”)
# library(“____”) (“UsingR”)

# HELP
?mean
# = help
??linear
# = functions in category

# Присвояване
a <- 4
b <- "FMI"
15 -> v

# Структури от данни
# Масиви
# Създаване на масив с функцията “c()”
v1 <- c()
# В конзолата излиза инпут
# Оттам може да се премахне стойност на промелива/функция с rm(“_”)
v1 <- c(1, 2, 3, 4)
v2 <- c("a", "b", "c")
v3 <- c(TRUE, FALSE, T, F)
